# Workflow syntax:
# https://help.github.com/en/articles/workflow-syntax-for-github-actions
name: cpp-linter

on:
  push:
    paths-ignore: "docs/**"

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  cpp-linter:
    runs-on: ubuntu-latest
    container:
      image: krisitanpn/teststar:base
      options: --user chipStarUser
    steps:
      - shell: bash
      - uses: actions/checkout@v2
      - name: Install clang
        run: sudo apt install -y clang-15 --install-suggests
      - name: Exploring
        run: pwd; echo $HOME; ls /home/chipStarUser
      - name: Exploring
        run: strings /usr/bin/bash | grep bashrc
      - name: Trying to get ml to work
        run:  ml; ml avail
      - name: C/C++ Linter
        uses: cpp-linter/cpp-linter-action@v2.9.0
        id: linter
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          style: file
          version: 15
          files-changed-only: false
          lines-changed-only: false
          extensions: 'cc,hh'
          tidy-checks: '-*,readability-*,modernize-*,clang-analyzer-*'
      - name: Fail fast?!
        if: steps.linter.outputs.checks-failed > 0
        run: exit 1

