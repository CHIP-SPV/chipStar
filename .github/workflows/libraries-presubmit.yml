name: Libraries Presubmit

on:
  pull_request:
    paths-ignore: "docs/**"

concurrency:
  group: libraries-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  build-and-test-libraries:
    runs-on: [self-hosted, Linux, X64]
    env:
      CHIP_MODULE_CACHE_DIR: ""
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0
          submodules: 'recursive'

      - name: Build chipStar + all libraries
        shell: bash
        run: |
          set -e
          if [ -f "$HOME/.local/init/bash" ]; then
            export MODULESHOME=$HOME/.local
            source "$MODULESHOME/init/bash"
          elif [ -f /etc/profile.d/lmod.sh ]; then
            source /etc/profile.d/lmod.sh
          else
            source /etc/profile.d/modules.sh
          fi
          module use /space/pvelesko/modulefiles
          module load oneapi/2025.0.4 llvm/22.0-native level-zero/dgpu
          module list

          STAGING=/tmp/chipstar-ci-staging/${{ github.event.pull_request.number }}
          rm -rf $STAGING

          ./install_chipstar.py --all \
            --with-tests \
            --install-dir $STAGING \
            --staging-dir $STAGING/staging \
            -j 8

          echo "STAGING=$STAGING" >> $GITHUB_ENV

      - name: Run library tests
        shell: bash
        run: |
          set -e
          if [ -f "$HOME/.local/init/bash" ]; then
            export MODULESHOME=$HOME/.local
            source "$MODULESHOME/init/bash"
          elif [ -f /etc/profile.d/lmod.sh ]; then
            source /etc/profile.d/lmod.sh
          else
            source /etc/profile.d/modules.sh
          fi
          module use /space/pvelesko/modulefiles
          module load oneapi/2025.0.4 llvm/22.0-native level-zero/dgpu

          # Find the chipStar install (date-stamped subdir)
          CHIP_INSTALL=$(ls -d $STAGING/chipStar/*/ | head -1)
          export PATH=$CHIP_INSTALL/bin:$PATH
          export LD_LIBRARY_PATH=$CHIP_INSTALL/lib:${LD_LIBRARY_PATH:-}
          export HIP_PATH=$CHIP_INSTALL
          export HIP_PLATFORM=spirv
          export CHIP_LOGLEVEL=err
          # Enable IGC software fp64 emulation on Intel Arc (Alchemist lacks native fp64)
          export IGC_EnableDPEmulation=1
          export OverrideDefaultFP64Settings=1

          failed=0
          # H4I-MKLShim's batch_correctness_test SEGFAULTs when run after
          # H4I-HipBLAS/FFT/SOLVER because those tests leave dirty SYCL/L0
          # context state on the GPU. Run H4I-MKLShim first (clean GPU state).
          all_libs=$(ls -d $STAGING/staging/*/ 2>/dev/null || true)
          mklshim_dir=$(echo "$all_libs" | grep "H4I-MKLShim" || true)
          other_libs=$(echo "$all_libs" | grep -v "H4I-MKLShim" || true)
          ordered_libs=$(
            [ -n "$mklshim_dir" ] && echo "$mklshim_dir"
            [ -n "$other_libs" ] && echo "$other_libs"
          )
          while IFS= read -r lib_staging; do
            [ -z "$lib_staging" ] && continue
            lib=$(basename $lib_staging)
            lib_build="$lib_staging/build"
            if [ ! -d "$lib_build" ]; then
              continue
            fi
            if [ -f "$lib_build/CTestTestfile.cmake" ]; then
              echo "=== Testing $lib ==="
              exclude=""
              exclude_file="$lib_staging/.ci/exclude.txt"
              if [ -f "$exclude_file" ]; then
                exclude=$(grep -Ev '^\s*(#|$)' "$exclude_file" | paste -sd '|' - || true)
              fi
              labels=""
              labels_file="$lib_staging/.ci/ctest-labels.txt"
              if [ -f "$labels_file" ]; then
                labels=$(grep -Ev '^\s*(#|$)' "$labels_file" | paste -sd '|' - || true)
              fi
              cd "$lib_build"
              if ! ctest --output-on-failure --timeout 1800 ${labels:+-L "$labels"} ${exclude:+-E "$exclude"}; then
                echo "::warning::$lib tests had failures"
                failed=$((failed + 1))
              fi
            else
              echo "=== $lib: no CTest tests ==="
            fi
          done <<< "$ordered_libs"
          if [ $failed -gt 0 ]; then
            echo "::error::$failed libraries had test failures"
            exit 1
          fi

      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: library-test-logs
          path: |
            /tmp/chipstar-ci-staging/${{ github.event.pull_request.number }}/staging/*/build/Testing/**/LastTest*.log
          if-no-files-found: ignore

  build-and-test-chipfft:
    runs-on: [self-hosted, Linux, X64]
    env:
      CHIP_MODULE_CACHE_DIR: ""
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0
          submodules: 'recursive'

      - name: Build chipStar + chipFFT
        shell: bash
        run: |
          set -e
          if [ -f "$HOME/.local/init/bash" ]; then
            export MODULESHOME=$HOME/.local
            source "$MODULESHOME/init/bash"
          elif [ -f /etc/profile.d/lmod.sh ]; then
            source /etc/profile.d/lmod.sh
          else
            source /etc/profile.d/modules.sh
          fi
          module use /space/pvelesko/modulefiles
          module load oneapi/2025.0.4 llvm/22.0-native level-zero/dgpu
          module list

          STAGING=/tmp/chipstar-ci-chipfft-staging/${{ github.event.pull_request.number }}
          rm -rf $STAGING

          ./install_chipstar.py --components chipfft \
            --with-tests \
            --install-dir $STAGING \
            --staging-dir $STAGING/staging \
            -j 8

          echo "STAGING=$STAGING" >> $GITHUB_ENV

      - name: Run chipFFT tests
        shell: bash
        run: |
          set -e
          if [ -f "$HOME/.local/init/bash" ]; then
            export MODULESHOME=$HOME/.local
            source "$MODULESHOME/init/bash"
          elif [ -f /etc/profile.d/lmod.sh ]; then
            source /etc/profile.d/lmod.sh
          else
            source /etc/profile.d/modules.sh
          fi
          module use /space/pvelesko/modulefiles
          module load oneapi/2025.0.4 llvm/22.0-native level-zero/dgpu

          CHIP_INSTALL=$(ls -d $STAGING/chipStar/*/ | head -1)
          export PATH=$CHIP_INSTALL/bin:$PATH
          export LD_LIBRARY_PATH=$CHIP_INSTALL/lib:${LD_LIBRARY_PATH:-}
          export HIP_PATH=$CHIP_INSTALL
          export HIP_PLATFORM=spirv
          export CHIP_LOGLEVEL=err
          export IGC_EnableDPEmulation=1
          export OverrideDefaultFP64Settings=1

          BUILD=$STAGING/staging/chipFFT/build
          if [ ! -f "$BUILD/CTestTestfile.cmake" ]; then
            echo "::error::chipFFT build directory missing or unconfigured: $BUILD"
            exit 1
          fi
          cd "$BUILD"
          ctest --output-on-failure --timeout 1800

      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: chipfft-test-logs
          path: |
            /tmp/chipstar-ci-chipfft-staging/${{ github.event.pull_request.number }}/staging/chipFFT/build/Testing/**/LastTest*.log
          if-no-files-found: ignore
